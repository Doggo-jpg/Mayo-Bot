//TODO make it anounce sht

module.exports ={
    name:'anounce',
    aliases:['anouncechannel','ac'],
    description:'[anuncio, ]',
    execute(message,args,cmd,client,Discord){
        

        if(cmd=== 'ac'||cmd==='announcechannel'){
            
        }
    }
}